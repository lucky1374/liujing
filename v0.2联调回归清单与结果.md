# v0.2 联调回归清单与结果

更新时间：2026-03-30

## 1. 回归范围

- 执行链路：任务 -> 脚本执行（lite / python）-> 执行记录 -> 缺陷 -> 报告
- Runner 链路：Python HTTP Runner 优先、失败回退本地执行
- 排障能力：Runner 诊断、快速定位建议、排查命令复制、联调速查入口

## 2. 自动验证结果

### 2.1 后端构建

- 命令：`cd /Users/lj/liujing/ai-test-platform && npm run build`
- 结果：通过

### 2.2 前端构建

- 命令：`cd /Users/lj/liujing/ai-test-platform-web && npm run build`
- 结果：通过
- 备注：存在大包体告警（不阻塞本轮联调）

### 2.3 Runner 健康检查

- 命令：`curl -i http://localhost:8001/health`
- 结果：`HTTP 200`，返回 `{"status":"ok"}`

### 2.4 Runner execute 探测

- 命令：
  - `curl -i -X POST "http://localhost:8001/execute" -H "Content-Type: application/json" -H "X-Runner-Token: change-me-runner-toke" -d '<probe-payload>'`
- 结果：`HTTP 200`，返回 `status=passed`

## 3. 人工回归清单（待执行）

请按下列顺序在平台界面执行并勾选：

- [ ] `lite` 脚本执行成功，执行来源显示 `Lite执行器`
- [ ] `python` 脚本执行成功，执行来源显示 `Python HTTP Runner`
- [ ] 人为制造 Runner 不可用后，执行来源显示 `Python 本地兜底`
- [ ] 人为制造 token 不一致时，执行结果失败并提示鉴权问题（不被本地兜底掩盖）
- [ ] 执行失败记录可“转缺陷”成功
- [ ] 基于任务生成测试报告成功
- [ ] “Runner诊断”弹窗可展示配置/health/execute 三项状态
- [ ] “复制诊断信息”可在联调群直接复用
- [ ] 失败执行记录“快速定位建议”展示正常
- [ ] “复制排查命令”可复制并可执行
- [ ] “联调故障速查”页面可访问，且“查看失败任务”可自动筛选 `status=failed`

## 4. 当前结论

- Runner HTTP 主链路已经可用。
- 联调故障定位链路（诊断 + 建议 + 命令 + 速查页）已具备。
- 下一步建议：完成上述人工回归勾选后，更新 `v0.2第一阶段总结.md` 并形成发布前验收记录。
