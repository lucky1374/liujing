# AI自动化测试平台上线前检查项（打勾版）

更新时间：2026-03-31

说明：

- `✅` 已完成并有结果支撑
- `🟡` 基本具备，发布前建议再过一遍
- `⬜` 待执行/待确认

## 1. 基础环境

- ⬜ MySQL 可访问（发布环境待现场确认）
- ⬜ Redis 可访问（发布环境待现场确认）
- ✅ 后端 `.env` 已配置（含 `PYTHON_RUNNER_URL`、`RUNNER_AUTH_TOKEN`）
- ✅ Python Runner `.venv` 已安装依赖并可执行
- ✅ 前后端依赖已安装完成，且构建通过（`npm run build`）
- ⬜ `docker-compose.local.yml` 一键启动待补实际验证

## 2. 数据与账号

- 🟡 数据库已初始化（开发环境已可用，发布库待复核）
- 🟡 管理员账号已创建（开发联调可用，发布账号待复核）
- ✅ 至少存在一个演示项目（`massage-service`）
- ✅ 至少存在一个演示环境（`dev`）

## 3. 功能联调

- ✅ 登录成功
- ✅ 项目管理可用
- ✅ 环境管理可用
- ✅ 用例管理可用
- ✅ 脚本管理可用
- ✅ 任务创建可用
- ✅ 任务执行可用
- ✅ 执行记录详情可用
- ✅ 失败转缺陷可用
- ✅ 测试报告生成可用

## 4. 真实能力验证

- ✅ 真实项目 `massage-service` 可切换
- ✅ 环境级 Bearer Token 配置生效
- ✅ GET 查询接口可执行
- ✅ POST 写操作接口可执行
- ✅ 顾客新增 + 查询闭环已验证
- ✅ Python Runner 模式已验证
- ✅ Python HTTP Runner 主链路命中成功（执行来源展示正常）
- ✅ Python 本地兜底链路可用（用于 Runner 异常兜底）

## 5. 可观测性与排障能力

- ✅ 后端已增强 Runner 调用日志（调用前/成功/失败）
- ✅ Runner 鉴权失败（401/403）不再被本地兜底掩盖
- ✅ `GET /test-tasks/runner/diagnostics` 诊断接口可用
- ✅ 前端 Runner 诊断状态卡可用
- ✅ 执行失败“快速定位建议”可用
- ✅ “复制排查命令”可用
- ✅ “联调故障速查”页面与入口可用
- ✅ “查看失败任务”快捷跳转与状态筛选可用

## 6. 安全与展示

- ✅ Bearer Token 页面不明文回填
- ✅ 执行详情请求头已脱敏
- ✅ 演示步骤文档已准备
- ✅ 已验证能力清单/回归清单/速查清单已准备

## 7. 暂不阻塞但需记录的问题

- 🟡 Runner 仍是单实例原型服务（暂未并发治理/队列）
- 🟡 容器隔离与监控未接入
- 🟡 UI 自动化执行尚未接入
- 🟡 前端存在大包体告警（不阻塞当前内部发布）

## 8. 发布前最小动作清单（建议 T-1 完成）

- 1) 在目标环境复跑一次：后端构建、前端构建、Runner `/health`、Runner `/execute` 探针
- 2) 用真实账号走一遍“任务执行 -> 失败转缺陷 -> 生成报告”
- 3) 固化发布环境 `RUNNER_AUTH_TOKEN` 与后端配置一致性
- 4) 将“联调故障速查清单”发给参与联调/演示的同学

## 9. 结论

当前版本已达到“可展示、可联调、可继续迭代”的内部发布条件；若完成第 8 节的发布前最小动作清单，可作为 v0.2 第一阶段收口版本对内发布。

## 10. 最新实操验证记录

- 验证时间：2026-03-31
- 自动项结果：
  - 后端构建通过
  - 前端构建通过
  - Runner `/health` 返回 200
  - Runner `/execute` 探针返回 200 且 `status=passed`
- 平台联调结果：
  - 后端 3000 端口恢复可用
  - 平台侧请求恢复正常，任务执行成功
