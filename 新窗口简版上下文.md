# 新窗口简版上下文（可直接复制使用）

最后更新：2026-04-14（持续维护模式已开启）

## 0) 维护约定（重要）
- 该文件作为新窗口唯一简版上下文，后续实现会边做边更新。
- 每次里程碑变化后，至少同步更新：
  - 当前阶段进度
  - 新增/变更接口
  - 关键改动文件
  - 下一步建议
- 若与代码不一致，以最新代码为准，并立即回写本文件。

## 1) 项目与技术栈
- 项目：AI 自动化测试平台
- 后端：`ai-test-platform`（Node.js + NestJS + TypeORM + MySQL + Redis + Bull）
- 前端：`ai-test-platform-web`（Vue3 + Element Plus）

## 2) 硬边界（必须遵守）
- 当前迭代优先接口自动化，不影响 UI 自动化模块。
- 不改 UI 自动化核心目录：`ai-test-platform-web/src/ai-test-agent/**`
- 不破坏 UI 自动化关键页面行为：`AiAgentLab`、UI 任务页入口与执行链路。

## 3) 当前阶段进度
- 阶段A（边界保护）：已完成
- 阶段B（定时调度）：已完成
- 阶段C（批量执行与编排）：已完成
- 阶段D（OpenAPI 导入）：已完成
- 阶段E（报告导出 CSV/Excel）：已完成
- 阶段F（接口集成测试 Scenario）：进行中（v1 最小可用已完成）
- 当前状态：可灰度发布，处于发布收口 + Scenario 能力迭代阶段
- 发布执行状态：
  - 后端灰度演练：已通过（预检 + 主链路验收通过）
  - 前端灰度验证：待执行/进行中（按发布清单顺序推进）
  - CI/CD 发布门禁：已接入（GitHub Actions 自动执行 preflight + acceptance）

## 4) 已完成能力（关键）
- 定时任务真实调度：注册/触发/恢复/幂等/调度日志
- 批量执行：多选执行、跨页按筛选执行、并发可配、批次历史与详情
- OpenAPI 导入：
  - 支持 JSON + YAML
  - 支持 Tag 批量勾选
  - 支持导入前编辑（用例名/模块/优先级）
  - 支持去重跳过（`skipExisting`）
  - 支持导入时配置“默认网关 base_url”，自动写入生成脚本
  - 支持导入时配置“默认请求头 JSON 模板”（如 Authorization），自动写入生成脚本
  - 支持按模块将导入脚本自动分组创建接口任务（`POST /test-cases/openapi/create-tasks`）
  - OpenAPI 弹窗“按模块创建任务”已调整为“先导入（强制创建脚本）再建任务”，避免仅预览时无脚本报错
  - OpenAPI 导入支持“导入后自动建任务”开关，导入所选可一键完成“导入 + 按模板建任务”
  - OpenAPI 弹窗新增“一键导入并执行”：可按配置完成“导入 -> 建任务 -> 批量执行”全流程
  - 一键执行后支持自动跳转到任务页并打开“批次历史”，高亮本次 batchId
  - 基线校验增强：支持“关键接口（METHOD /path）必须通过”校验（执行后基于本次记录核验）
  - 一键流程支持“执行后自动导出报告CSV”（按本次任务ID集合导出）
  - TestTasks 新增“回归健康摘要（最近批次）”卡片：展示最近批次通过率、失败数、基线是否达标
  - 去重兼容增强：`skipExisting` 除 `remarks=openapiKey:*` 外，新增基于 `testSteps` 的历史数据识别（适配旧导入数据）
  - 预览体验增强：勾选 `skipExisting` 时，OpenAPI 预览列表仅展示“新接口”，避免误解为跳过无效
- 报告导出：
  - 单报告导出 `CSV / XLSX`
  - 按筛选条件导出报告列表 `CSV`
- 线上联调修复：已批量将 OpenAPI 导入脚本中的 `base_url` 从 `http://localhost:3000` 替换为 `https://ananx-qa.juhux.com`（本次更新 99 条，另 1 条已手工改过）
- 接口集成测试（Scenario v1）：
  - 支持场景管理（创建/查询/详情/更新/删除）
  - 支持步骤串行执行（按步骤数组顺序）
  - 支持变量传递（`extractors` 提取并写入上下文变量，后续步骤可通过 `{{var}}` 使用）
  - 支持基础断言（`eq` / `contains` / `exists`，来源支持 `response.status` / `response.body` / `response.headers` / `vars`）
  - 支持执行记录落库（输入变量、输出变量、步骤结果、耗时、失败原因）
  - 前端最小入口已上线（场景列表 + 执行 + 执行记录查看）
  - 前端增强：支持可视化步骤编辑器（与 JSON 双向同步）、失败执行一键重跑
  - 与测试任务联动：支持“场景转任务”（可选定时回归），任务执行链路可直接执行 Scenario
  - 断言/提取增强：支持 `pathType=jsonpath` 与正则提取/断言（`regex`）
  - 条件分支：支持按变量/响应结果跳转步骤（`branchRules` / `onPassedStepIndex` / `onFailedStepIndex`，含循环保护）
  - 定时回归看板：支持 Scenario 任务趋势、失败原因 Top、失败任务排行
  - 失败重试策略：支持按失败原因自动重试/跳过重试（任务级 `scenarioRetryConfig`）
  - 用例模板库：支持模板列表与一键创建（鉴权、下单、退款、库存、优惠券、履约六类模板）
  - 重试监控：支持重试次数分布、重试后成功/失败、重试后失败原因 Top
  - 模板参数可视化：模板创建支持按变量类型自动生成表单（string/number/boolean/json），并与 JSON 覆盖双向同步
  - 重试策略高级化：支持指数退避（`backoffBaseMs/backoffMaxMs`）与按失败类型重试（`retryOnFailureTypes/skipRetryOnFailureTypes`）
  - 模板共享：支持模板导出/导入（可覆盖同key模板，便于团队共享版本）
  - 重试策略推荐集：支持按业务线一键套用（电商/履约/营销）
  - 模板审批流：支持提交审核、审核通过/驳回、待审核队列（自定义模板）
  - 执行成本分析：支持按重试次数与耗时分析任务成本（总尝试次数、额外重试成本占比、高成本任务Top）
  - 模板质量评分：支持模板质量分与分项评分（结构/断言/变量依赖/可维护性），支持质量报告
  - 审核记录归档：支持模板审核历史与最近两次差异对比（变更路径级）
  - 评分规则可配置：支持按业务线配置质量评分权重（结构/断言/变量依赖/可维护）
  - 生命周期治理：支持模板启用/废弃/归档与治理提醒（待审核超时、废弃过久、驳回未处理）
  - 成本质量联合预警：支持识别“高重试成本 + 低质量评分”任务并给出优化建议
  - 模板发布自动化：审核通过后自动生成发布标签（`releaseTag`）并记录发布时间
  - 生命周期提醒通知：支持通过 Webhook / 邮件网关发送治理提醒（支持 dryRun 预览）
  - 发布门禁联动：门禁页新增“模板发布通告”区块，展示最近发布标签与发布说明
  - 预警闭环：生命周期提醒发送结果支持回写到模板治理审计日志（动作 `reminder_notified`）
  - 发布审计报表：支持发布动作时间线与动作统计（通过/发布/驳回/提醒）
- AI 报告复盘卡片：支持生成结构化总结（风险点/根因候选/回归建议/下一步）并人工确认保存
- AI 缺陷根因分析 v1：支持结构化缺陷分析卡片生成与人工确认（根因候选/修复建议/相似缺陷洞察）
- AI 缺陷相似案例检索增强 v2：支持 hybrid/lexical 两种检索模式、可配置 topK/候选池、相似度分解（lexical/ngram/signature/metadata）与历史解决方案建议（含置信度）
- AI 根因分析建议回灌：AI分析支持可选开关将历史建议回灌至 `fixSuggestions/nextActions`，并保留 `similarRecommendation` 结构化字段
- 检索效果评估：新增缺陷相似检索效果指标接口（命中率、平均相似度、高置信建议采纳率）
- 建议采纳显式确认：AI分析确认时支持 `adoptSimilarRecommendation`，用于沉淀“高置信建议采纳率”指标
- 指标趋势可视化：缺陷页与发布门禁页均支持 7/30 天切换与趋势柱图（命中率/高置信采纳率）
- AI门禁联动：发布门禁新增“高置信建议采纳率阈值”规则（样本>=5时参与发布结论）
- 门禁阈值持久化：发布门禁支持按项目保存阈值配置（通过率阈值/AI采纳率阈值/AI样本阈值）
- 门禁建议动作：相似检索指标返回 `suggestedActions`，发布门禁页展示建议动作用于失败处置
- 团队共享门禁配置：阈值配置已从前端本地存储升级为项目后端配置（可多人共享）
- 门禁阈值审计：支持记录并展示阈值变更历史（时间/操作人/前后阈值）
- 门禁一键定位：发布门禁页支持一键跳转缺陷页并按待处理状态筛选
- 门禁精准定位：一键跳转缺陷时支持附带“未采纳历史建议 + 高置信度 + 最近N天”筛选参数
- 门禁变更原因：阈值保存支持 `changeReason`，审计列表展示变更原因
- 门禁整改单草稿增强：支持预填负责人/优先级/标签，支持纯文本与 Markdown 双格式复制
- 门禁工单JSON：整改单草稿弹窗支持一键复制工单 JSON（title/priority/owner/labels/description 等）
- 门禁工单JSON增强：新增 `sourceType="release-gate"` 与 `generatedAt` 字段，便于外部系统追踪
- 项目级整改单模板默认值：`owner/priority/tags` 已纳入项目门禁配置并支持审计
- Phase 2 收口文档：已补充统一指标口径与回归检查清单
- 平台实链验收完成（按按v0.1.2）：关键接口与门禁链路验证通过，验收后已回滚配置基线

## 5) 关键接口（新增/核心）
- 任务调度健康：`GET /test-tasks/scheduler/health`
- 任务调度日志：`GET /test-tasks/:id/schedule-logs`
- 批量执行：`POST /test-tasks/batch-execute`
- 跨页批量执行：`POST /test-tasks/batch-execute-by-query`
- 批次历史：`GET /test-tasks/batch-executions`
- 批次详情：`GET /test-tasks/batch-executions/:batchId`
- OpenAPI 预览：`POST /test-cases/openapi/preview`
- OpenAPI 导入：`POST /test-cases/openapi/import`
- 单报告导出：`GET /test-reports/:id/export?format=csv|xlsx`
- 报告筛选导出：`GET /test-reports/export?projectId=&relatedTaskId=&type=&maxRows=`
- 场景管理：`POST /integration-scenarios`
- 场景列表：`GET /integration-scenarios`
- 场景详情：`GET /integration-scenarios/:id`
- 场景更新：`PUT /integration-scenarios/:id`
- 场景删除：`DELETE /integration-scenarios/:id`
- 场景执行：`POST /integration-scenarios/:id/execute`
- 场景执行历史：`GET /integration-scenarios/:id/executions`
- 场景执行详情：`GET /integration-scenarios/executions/:executionId`
- 场景模板列表：`GET /integration-scenarios/templates`
- 模板创建场景：`POST /integration-scenarios/templates/:templateKey`
- 模板导出：`GET /integration-scenarios/templates/export`
- 模板导入：`POST /integration-scenarios/templates/import`
- 模板待审核队列：`GET /integration-scenarios/templates/review-queue`
- 模板提交审核：`POST /integration-scenarios/templates/:templateKey/submit`
- 模板审核：`POST /integration-scenarios/templates/:templateKey/review`
- 模板质量报告：`GET /integration-scenarios/templates/quality-report`
- 模板审核归档：`GET /integration-scenarios/templates/:templateKey/audits`
- 模板审核差异：`GET /integration-scenarios/templates/:templateKey/audits/diff`
- 质量评分策略列表：`GET /integration-scenarios/templates/quality-policies`
- 质量评分策略更新：`PUT /integration-scenarios/templates/quality-policies/:businessLine`
- 生命周期更新：`PUT /integration-scenarios/templates/:templateKey/lifecycle`
- 生命周期提醒：`GET /integration-scenarios/templates/lifecycle-reminders`
- 成本质量预警：`GET /test-tasks/observability/scenario-cost-quality-alerts?projectId=&days=`
- 模板发布信息：模板列表返回 `releaseTag/releasedAt/releaseNote`
- 生命周期提醒通知：`POST /integration-scenarios/templates/lifecycle-reminders/notify`
- 模板发布通告：`GET /integration-scenarios/templates/release-notices?limit=`
- 预警回写：生命周期提醒通知后自动写入模板审计归档
- 发布审计报表：`GET /integration-scenarios/templates/release-audit-report?days=&limit=`
- AI报告总结生成：`POST /test-reports/:id/ai-summary/generate`
- AI报告总结确认：`POST /test-reports/:id/ai-summary/confirm`
- AI缺陷分析生成：`POST /defects/:id/analyze`
- AI缺陷分析确认：`POST /defects/:id/analyze/confirm`
- AI缺陷分析确认扩展：`POST /defects/:id/analyze/confirm`（支持 `adoptSimilarRecommendation`）
- AI缺陷相似案例增强检索：`GET /defects/:id/similar-cases?topK=&mode=hybrid|lexical&candidatePoolSize=&allowCrossProject=`
- AI缺陷相似检索指标：`GET /defects/similarity-metrics?projectId=&days=&minScore=`
- AI缺陷相似检索指标扩展：`GET /defects/similarity-metrics?projectId=&days=&minScore=&includeTrend=true`
- AI缺陷相似检索指标扩展：`GET /defects/similarity-metrics?projectId=&days=&minScore=&includeTrend=true`（返回 `suggestedActions`）
- 项目门禁阈值配置获取：`GET /projects/:id/release-gate-settings`
- 项目门禁阈值配置更新：`PUT /projects/:id/release-gate-settings`
- 项目门禁阈值变更记录：`GET /projects/:id/release-gate-settings/audits?limit=`
- 项目门禁阈值配置更新扩展：`PUT /projects/:id/release-gate-settings`（支持 `changeReason`）
- 项目门禁阈值配置更新扩展：`PUT /projects/:id/release-gate-settings`（支持 `changeReason` + `rectificationOwnerDefault/rectificationPriorityDefault/rectificationTagsDefault`）
- 缺陷列表增强筛选：`GET /defects?aiRecommendationAdopted=true|false&minRecommendationConfidence=&days=`
- 重试监控概览：`GET /test-tasks/observability/scenario-retry-overview?projectId=&days=`
- 重试策略推荐集：`GET /test-tasks/observability/scenario-retry-presets`
- 成本分析：`GET /test-tasks/observability/scenario-cost-analysis?projectId=&days=`

## 6) 关键文件（本轮实现主要改动）
- 后端：
  - `ai-test-platform/src/modules/test-task/test-task-scheduler.service.ts`
  - `ai-test-platform/src/modules/test-task/test-task.processor.ts`
  - `ai-test-platform/src/modules/test-task/test-execution.service.ts`
  - `ai-test-platform/src/modules/test-task/test-task.controller.ts`
  - `ai-test-platform/src/modules/test-task/test-task.module.ts`
  - `ai-test-platform/src/modules/project/entities/project.entity.ts`
  - `ai-test-platform/src/modules/project/dto/project.dto.ts`
  - `ai-test-platform/src/modules/project/project.service.ts`
  - `ai-test-platform/PHASE2_CLOSURE_METRICS_AND_REGRESSION.md`
  - `ai-test-platform/PHASE2_PLATFORM_ACCEPTANCE_2026-04-14.md`
  - `ai-test-platform/src/modules/test-task/test-task.controller.ts`
  - `ai-test-platform/src/modules/test-task/test-execution.service.ts`
  - `ai-test-platform/src/modules/test-task/entities/test-task.entity.ts`
  - `ai-test-platform/src/modules/test-task/dto/test-task.dto.ts`
  - `ai-test-platform-web/src/views/IntegrationScenarios.vue`（转任务弹窗支持重试配置）
  - `ai-test-platform/src/modules/test-task/entities/task-schedule-log.entity.ts`
  - `ai-test-platform/src/modules/test-task/entities/task-batch-execution.entity.ts`
  - `ai-test-platform/src/modules/test-case/test-case.service.ts`
  - `ai-test-platform/src/modules/test-case/test-case.controller.ts`
  - `ai-test-platform/src/modules/test-case/dto/test-case.dto.ts`
  - `ai-test-platform/src/modules/test-report/test-report.service.ts`
  - `ai-test-platform/src/modules/test-report/test-report.controller.ts`
  - `ai-test-platform/src/modules/test-report/dto/test-report.dto.ts`
  - `ai-test-platform/src/modules/integration-scenario/integration-scenario.module.ts`
  - `ai-test-platform/src/modules/integration-scenario/integration-scenario.controller.ts`
  - `ai-test-platform/src/modules/integration-scenario/integration-scenario.service.ts`
  - `ai-test-platform/src/modules/integration-scenario/scenario-execution.engine.ts`
  - `ai-test-platform/src/modules/integration-scenario/entities/integration-scenario.entity.ts`
  - `ai-test-platform/src/modules/integration-scenario/entities/integration-scenario-execution.entity.ts`
  - `ai-test-platform/src/modules/integration-scenario/dto/integration-scenario.dto.ts`
  - `ai-test-platform/src/modules/integration-scenario/scenario-execution.engine.spec.ts`
  - `ai-test-platform/src/modules/integration-scenario/scenario-templates.ts`
  - `ai-test-platform-web/src/views/IntegrationScenarios.vue`（步骤编辑器支持分支字段配置）
  - `ai-test-platform/src/modules/test-task/test-execution.service.ts`
  - `ai-test-platform/src/modules/test-task/test-task.service.ts`
  - `ai-test-platform/src/modules/test-task/entities/test-task.entity.ts`
  - `ai-test-platform/src/modules/test-task/dto/test-task.dto.ts`
  - `ai-test-platform/src/modules/test-task/test-task.module.ts`
- 前端：
  - `ai-test-platform-web/src/views/TestTasks.vue`
  - `ai-test-platform-web/src/views/TestCases.vue`
  - `ai-test-platform-web/src/views/TestReports.vue`
  - `ai-test-platform-web/src/views/IntegrationScenarios.vue`
  - `ai-test-platform-web/src/router/index.js`
  - `ai-test-platform-web/src/views/Layout.vue`
  - `ai-test-platform-web/src/views/ReleaseGate.vue`
- 运维脚本：
  - `start-local.sh`
  - `stop-local.sh`
- CI/CD：
  - `.github/workflows/release-gate.yml`

## 7) 发布与运维文档
- 发布回滚清单：`liujing/上线发布与回滚清单-v1.md`
- 生产环境模板：`ai-test-platform/.env.release.template`
- 外部业务系统 OpenAPI 转换产物：`liujing/massage-service-openapi3.json`（来源：`https://api-doc-dev.haoduo.vip/api/v1/massage-service/v2/api-docs`）
- 本地可访问导入 URL：`http://127.0.0.1:8899/massage-service-openapi3.json`（由本地静态服务提供）
- 回归模板文档：`liujing/按按v0.1.2-回归任务模板.md`
- 一键回归SOP（最终版）：`liujing/按按v0.1.2-一键回归SOP-最终版.md`

## 8) 快速验收命令
- 预检：`SKIP_BUILD=true ./preflight-check.sh`
- 主链路验收：`CLEANUP=true ./acceptance-workflow.sh`
- 定时批量回归脚本：`liujing/run-regression-cron.sh`

## 9) 下一步建议（新窗口可继续）
1. OpenAPI 导入增加“覆盖更新模式”（非仅跳过）
2. Scenario 模板治理自动化规则（按提醒级别自动流转状态）
3. AI 缺陷相似案例检索增强（向量检索 + 历史解决方案推荐）✅（已完成 v2：hybrid 相似度 + 历史建议）
4. Phase 2 收口建议：按 `PHASE2_CLOSURE_METRICS_AND_REGRESSION.md` 执行一次完整回归并固化发布模板

## 10) 近期运维操作记录
- 已在项目 `96d7bbc9-c1d8-472c-a974-533e97658a90` 删除重复导入的 100 条测试用例（仅删用例，不改脚本与任务）。
- 删除清单：`liujing/.local-run/deleted-duplicate-testcases.json`
- 已在同项目删除重复导入的 100 条测试脚本（仅删除未被任务引用的重复脚本，避免影响任务执行）。
- 删除清单：`liujing/.local-run/deleted-duplicate-testscripts.json`
- 已按要求清空项目 `按按v0.1.2`（`96d7bbc9-c1d8-472c-a974-533e97658a90`）下全部测试任务/测试脚本/测试用例，用于重新 OpenAPI 导入。
- 清空结果：任务 43、脚本 264、用例 264 全部删除；删除后均为 0。
- 清空清单：`liujing/.local-run/project-v012-purge-result.json`
- 已批量将项目 `按按v0.1.2` 下 264 条测试脚本的 `base_url` 替换为 `https://ananx-qa.juhux.com`。
- 替换清单：`liujing/.local-run/replace-base-url-v012.json`
- 已从 MySQL binlog 中恢复 1 条被误删 UI 任务（项目 `按按v0.1.2`，任务名 `UI`），新任务ID：`754fd036-925b-4e8b-bf60-a32c6bd0b1bf`。

## 11) 治理收尾进展
- 已上线“防误删二次确认”：删除项目/任务/脚本/用例时，必须输入同名名称才允许删除。
- 已上线“一键导入执行日志落盘”：本地 localStorage 保留最近 100 条流水记录，支持 CSV 导出。

## 12) 运营化能力
- 已新增“发布门禁”页面：`/release-gate`，可基于最近批次通过率与失败数给出“允许发布/禁止发布”结论。
- 发布门禁支持本地记录落盘与 CSV 导出（最近 200 条门禁检查记录）。

---

## 给新窗口的开场提示词（可直接复制）
你是我的协作开发助手。项目是 `liujing/ai-test-platform`（NestJS）和 `liujing/ai-test-platform-web`（Vue3）。
请先读取 `liujing/新窗口简版上下文.md`，严格遵守其中“UI 自动化模块不受影响”的边界，再继续实现未完成事项。
